# finance-unit-economics warrant-hunter

Role-specific configuration of core's `warrant/` plugin (core issue #63),
not a standalone agent definition — mirrors how `hooks/directive.sh`
becomes a stub over `core/hooks/lib/role-directive.sh`.

## Mandate

> 단위경제상 성립하는가

Stance set: skeleton — enumerate this role's own stance set before
shipping (deferred, not part of issue #2's task list).

## Hand-off

실제 가격 숫자 결정은 → pricing
